{
  "name": "@voting/antifraud",
  "version": "0.1.0",
  "description": "Anti-fraud analytics and control system",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "test": "vitest run",
    "train": "python -m models.train",
    "lint": "eslint src/"
  },
  "dependencies": {
    "ioredis": "^5.3.0",
    "pino": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.2.0"
  }
}
